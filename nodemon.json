{
  "watch": ["src"],
  "ext": "ts",
  "exec": "sh -c 'pkill -9 -f \"src/main.ts\" || true; while lsof -t -i:3000 >/dev/null; do echo \"[System] Port 3000 busy, killing...\"; lsof -t -i:3000 | xargs kill -9; sleep 0.5; done; node -r ts-node/register -r tsconfig-paths/register src/main.ts'",
  "ignore": ["src/**/*.spec.ts"],
  "signal": "SIGUSR2",
  "legacyWatch": true,
  "delay": 2000
}
